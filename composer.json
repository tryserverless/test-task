{
  "name": "tryserverless/test-task",
  "description": "Test task",
  "autoload": {
    "classmap": [
      "api/"
    ]
  },
  "require-dev": {
    "phpunit/phpunit": "^9"
  },
  "scripts": {
    "test": "./vendor/bin/phpunit --bootstrap vendor/autoload.php tests/",
    "testdox": "./vendor/bin/phpunit --bootstrap vendor/autoload.php --testdox --colors=always tests/"
  },
  "license": "MIT"
}
